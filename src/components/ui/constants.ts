import { FONT } from "@/constants/theme";

export const inputCls = "w-full px-4 py-3 rounded-xl bg-white text-sm outline-none transition-colors";
export const inputStyle = { fontFamily: FONT, boxShadow: "0 0 0 1px rgba(26,17,8,0.12)" } as const;
